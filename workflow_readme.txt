workflow_readme

We were able to get workflow.py to run without returning any errors in the command line but when it would complete, no workflow dataset appeared in Big Query. Running workflow.py took about 15 minutes with no results. 